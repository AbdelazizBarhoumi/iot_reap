// Removed: ProtectedRoute (test-only, never used in production)
// Removed: RoleRoute (never imported anywhere)

export {
    HasRole,
    IsAdmin,
    IsTeacher,
    IsEngineer,
    IsSecurityOfficer,
    useRole,
} from './HasRole';

