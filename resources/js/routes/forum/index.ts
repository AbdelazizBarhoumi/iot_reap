import trainingUnit from './trainingUnit'
import trainingPath from './trainingPath'
import threads from './threads'
import replies from './replies'
const forum = {
    trainingUnit: Object.assign(trainingUnit, trainingUnit),
trainingPath: Object.assign(trainingPath, trainingPath),
threads: Object.assign(threads, threads),
replies: Object.assign(replies, replies),
}

export default forum